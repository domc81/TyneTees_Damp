# TyneTees Damp — Project State

**Last updated:** 2026-07-26
**Last commit:** 7fabd2f — fix(theme): raise muted-text alpha floor on light — Kanban cards readable
**Current phase:** Warmline discoverability (pt 4) COMPLETE; Phase 3 operative outputs (pt 15) COMPLETE — contractor engine parity-gated to the workbook U/V columns (15/15); Phase 1 complete; remaining review work = payment staging (pt 16)

## Current focus

**Costing engine trust restored (2026-07-11, review response).** Steven's 36pp technical review (`docs/DOM_Survey_Report_and_Costing_Platform_Technical_Review.pdf`) found the costing layer untrusted (£638.41 benchmark variance, abandoned live survey). Response, all shipped same day: golden-master parity harness (`parity/`, README there) → rates audit (0 wrong primary rates; 70 wastage misapplications + truncations fixed, migration 1) → code batches 2-4 (DPC algebra + thickness-in-metres, resin components, tape/technoseal/rope/bags, core-hole independence) → full-coverage campaign (every priced row of all 4 workbooks capturable + penny-exact, migrations 2-5) → admin backend audit (dead controls removed) → **pricing control fully admin-wired** (map + gap fixes: `docs/workbook-analysis/PRICING_CONTROL_MAP.md`, migration 6 — the client now edits every price at /admin/rates, /admin/materials, /admin/costing without developer involvement, the workbooks' core complaint). **The parity suite is the release gate for any costing change** (commands in `AGENTS.md`). Evidence pack for Steve: `parity/audit/` — incl. two workbook defects to report (damp totals omit its asbestos section; woodworm prep hours subtotal omits wallpaper+antinox).

**Phase 1 customer-facing fixes shipped 2026-07-12** (plan: `docs/plans/2026-07-11-phase1-customer-facing-fixes.md`; commits 92480e4→1bb55a9): all 8 items implemented, deployed and verified on the live site (public pages via curl/UA matrix, auth-gated costing/wizard UI via Steel Browser on a phone viewport). Decisions taken per the plan's recommendations: D1=B (PSO as its own customer line), D2=(a) (report stays link; quotation PDF attached), D3=all rows backfilled. **D4 (verified tyneteesdampproofing.co.uk from-address) is blocked**: that domain is not in our Cloudflare account — needs Dom/client DNS access before Resend domain verification.

**Next up:** send Steve the evidence pack + book the working session (agenda now also carries: Warmline methodology wording sign-off + his pt-4 acceptance test, the DPC-cream rounding asymmetry, whether to author timber/woodworm purchase rules — their workbook mats sheets are "TBC" — and the five workbook contractor-column/summation defects); confirm the /q Edge fix on Steven's actual Windows laptop (his tests 40-42 — server side proved healthy under Edge UAs, `client_errors` captures anything residual); then payment staging (review pt 16), the last open review phase.

**Canonical path (2026-07-05):** every survey starts as a lead in the pipeline (`/survey/new` deleted); repeat customers via existing-customer search, free revisits via Convert & Book "No survey fee". /surveys ("Projects") is purely the view/manage list.

## Open threads

- **Offline PWA — team testing phase (started 2026-07-06):** iPhone T10 passed after the bc75793 fix round (airplane-mode open + tap-through into downloaded surveys verified by Dominic). Still to validate with Steve's team — T10 on Android (primary platform), T14 (rural field pilot; capture sync-pill comprehension; if the stale nudge fires often, consider Background Sync API as an Android-only enhancement), T15 (real Android tablet pass of all 5 steps + PhotoCapture + AudioRecorder + pill), and real mic→queue voice-note recording (couldn't be exercised headlessly — the transcription-flush half is verified). Offer stands to add a "Working offline — daily routine" section to the surveyor training guide once the process settles.
- **CF replacement (PARKED 2026-07-05 — revisit from late July 2026):** confirmed direction — this platform will replace Contractor Foreman entirely once its used feature set is mapped and built. Agreed approach when resumed: audit-first — map the client's live CF account via Steel Browser (module inventory with usage evidence, workflow maps, data/migration inventory → `docs/cf-audit/`), then gap-analyse against the platform, then a phased build plan (likely project/works management first; invoicing + payments second, blocked on Steven's answers: bookkeeping software, payment provider, invoicing structure, operative count, offline needs). Explicitly decided against: Steel Browser automation of CF project creation from the handover pack (feasible but throwaway against a product being retired). The CSV handover pack remains the bridge. Prerequisites to resume: CF login credentials (`~/.credentials/.contractor-foreman-credentials`), 2FA status, a supervised first session at browser.dc81.io.
- **Phase 1 follow-ups (shipped 2026-07-12, three loose ends):** (1) Edge confirmation on Steven's actual laptop — check `client_errors` for anything his browser reports; (2) D4 verified from-address — path established 2026-07-12: their DNS is at Bluehost (managed by Wayne), mail on Microsoft 365, DMARC p=none to wayne@. Three additive records authorise Resend (MX+SPF on send. subdomain, DKIM selector) with zero impact on their mail; draft email for Steve/Wayne prepared. Domain registered in the shared Resend account 2026-07-12 (plan upgraded by Dom; domain id 6347a750-8d3b-4f89-b7ce-bb16925e6ad3, eu-west-1, status not_started) — record set incl. real DKIM key sent to Steve 2026-07-12; awaiting Wayne's Bluehost records + Steven's from-address choice (proposed quotes@), then verify via API (POST /domains/6347a750…/verify) and set platform_from_email (platform's restricted sending key sends from any verified domain on the account); (3) D2 follow-up — report-PDF pipeline (headless print) if Steven wants a report attachment rather than the link + print-friendly page.
- **Review phases still open:** payment staging (pt 16 — 30% / 50%-of-remainder / final 50%, kill "deposit" wording; the frozen "Deposit required" labels in the costing summary and quotation views are tagged with pt-16 comments). Pt 4 follow-ups: Steven to sign off the drafted `METHODOLOGY_WARMLINE` report wording, and to run his acceptance test (repeat the aborted live survey on-platform, penny-check vs workbook). Pt 15 follow-ups: operative LOGIN tier (review permission model has operatives receiving instructions, not logging in — printed/PDF work instruction ships it; a login role is a future decision), sketch attachment on the work instruction, and THREE new workbook contractor-column defects to report to Steve alongside the two known summation defects (woodworm U pays operatives 110% of skip/transfer invoices; damp resin U compound formula pays £7.71 for £127 of resin; damp U/V totals omit asbestos).
- **Costing follow-ups (parity-gated, see `parity/audit/CAPTURE_GAPS.md`):** catalog re-link of the 7 deactivated rows is **on-client-demand only, 4 of 7 max** (decided 2026-07-11 — rationale in `docs/workbook-analysis/PRICING_CONTROL_MAP.md` §3; the lines are already fully editable at /admin/costing); `price_divisor` param for the 6 pack-representation quirks; digital-DPC radius/construction record fields; line-level override UI (engine supports `input.overrides`, no UI).
- **Legacy survey data:** TT-2026-0029 still stores the halved 20 m² resin workaround + `dpc_wall_depth: 1` courses — surveyor should re-enter (wizard prompts); other pre-parity surveys recost via documented legacy fallbacks until edited.
- **Survey type refactor:** `survey_type` enum includes `structural`, `comprehensive`, `site_preparation` — `site_preparation` has 3 costing sections but no wizard steps or report templates; the other two have nothing. Selecting them creates dead-end surveys. Plan exists at `docs/plans/2026-03-02-survey-type-display-migration.md`.
- **Role-based RLS tightening:** the four pricing tables are now DB-gated (admin-only writes via `is_pricing_admin()`, migration `20260711000007`) — the highest-value slice done. Remaining tables still grant full access to all authenticated users; app-level route guards and API role checks mitigate. Full RLS migration deferred — acceptable for small team but must fix before team growth.
- **API route role checks:** payment mark-paid/send-link, quotation generation, company profile, and logo endpoints now require admin/office role (fixed in both audits). LLM/transcription endpoints remain open to all authenticated users (acceptable — no PII exposure, API credit risk is low with single-user team).
- **Supabase Auth SMTP not configured:** `GOTRUE_SMTP_HOST`, `GOTRUE_SMTP_USER`, `GOTRUE_SMTP_PASS`, `GOTRUE_SMTP_ADMIN_EMAIL` are all blank on the TTDP auth container. Password reset emails (`resetPasswordForEmail()`) silently fail. Requires root-session fix to update Coolify environment variables with SMTP credentials (Resend SMTP is available). Workaround: admin resets passwords via Supabase Admin API + `must_change_password` flag.
- **Payment provider integration:** deliberately deferred — client is not ready. Survey fees are paid through the client's external payment system and office marks them paid manually; deposits are taken in Contractor Foreman after handover. The `/pay/[token]` page and `/api/payments/send-link` route stay dormant until a provider is integrated (fix send-link's empty `appUrl` fallback before first activation). Architecture is Stripe-ready (fields in payments table).
- **UX audit follow-ups:** both audits fully closed — every platform and customer finding fixed and verified live (see Recently shipped; reports in `docs/audits/`). Untested remainder from the customer audit: email bodies (no inbox access), decline path end-to-end, expired-link states, quote PDF/print output — candidates for a future pass. Steven Robinson duplicates deduped 2026-07-05 (history re-pointed to canonical profile `8bb49fa5`; two test profiles + auth users deleted). Training guides still describe pre-unification pipeline columns in places — content refresh needed.
- **Light theme shakedown:** have Steven and the office run the toggle on their own devices during normal use for a day — any surface that still reads weak in light mode is a one-line token fix (pipeline board pass done 2026-07-26; muted-text alpha floor now applies platform-wide).
- **Release-unpaid-bookings cron:** `/api/cron/release-unpaid-bookings` needs a Coolify scheduled task (daily, e.g. 09:00) calling `POST` with `Authorization: Bearer $CRON_SECRET`.

## Known issues

- **13 survey-type extension tables unused:** schema provisioned but wizard uses JSONB instead. Candidates for removal.
- **RLS policies still grant full access on non-pricing tables:** `USING (true) WITH CHECK (true)` on core tables (pricing tables fixed 2026-07-11; `client_errors` + `company_locations` created service-role-only 2026-07-12). App-level guards mitigate risk but DB-level isolation needs a dedicated migration session.
- **Legacy quotation snapshots carry penny drift:** section rows vs stored `total − VAT` differ by up to ~3p on pre-parity quotations (e.g. QT rows sum £5,028.40 vs £5,028.43 stored). The customer view stays internally consistent; drift is logged by `quotation-presentation.ts`'s reconciliation guard, never blocks a render. Post-parity quotations reconcile exactly.

## Recently shipped

- 2026-07-26 — Light-theme legibility for the pipeline board (d9784ba + 7fabd2f, user-reported): Kanban cards were unreadable in light mode — inline `rgba(255,255,255,…)` card faces, raw hex badge colours, a `bg-gray-900` popover, and ≤40%-alpha grey text/icons. Fixed at the token layer: `.kanban-card` classes (elevated white cards on light), `themedHex()` + `--tt-hex-mix` (color-mix darkens dynamic hexes), `--tt-actions-idle` icon-bar idle opacity, and a platform-wide light-mode alpha floor for muted ink text (`text-white/15`…`/60` + placeholders). Dark theme unchanged bar `font-medium` on thin card meta; rules recorded in the AGENTS.md theming gotcha.
- 2026-07-25 — Light/dark theme toggle (5a83aba, client request): sidebar toggle switches the whole internal app between the existing dark theme (default) and a new light theme; persisted per device (`ttdp-theme` localStorage, applied pre-paint — no flash, works in the offline wizard). Built as a central token layer (Tailwind `white` = ink token + 300/400 status-shade remaps, `--tt-*` vars in globals.css) so all ~3k existing colour utilities adapt with no per-page rework. Customer pages (/q, /pay, /report incl. Customer Preview) are pinned to their original rendering via `.theme-native` wrapper layouts. Theming rules: AGENTS.md gotcha.
- 2026-07-20 — Steven's 3 live-use issues fixed (c525139→3c3b9f2): extraction count fields (Electrical Packs / Core Holes / Fan Grilles + passive vent counts) no longer snap back to 0 when cleared; report editor gains a **Regenerate** button (rebuilds all sections from current survey data into the same row — publish token/send history survive, status back to `generated`) plus completeness-panel warnings when room photos or surveyor comments post-date generation (root cause on TT-2026-0035: report generated+finalised 28s apart, defect photos + extended comments arrived an hour later — office should press Regenerate there); editor sub-sections now render their photos (were hardcoded `photos={[]}`); Surveyor's Additional Comments gains voice dictation + Polish/undo, reusing the existing `{kind:'wizard'}` offline transcription target. Follow-up same day (0d9618f): **Customer Preview** — `/report/[id]?preview=1` renders the real public page for staff sessions (banner, no view tracking) so the office sees the exact customer document before finalising; button beside Regenerate in the report editor.
- 2026-07-15 — Pipeline contact edits now propagate (f5b553a, resync migration `20260715000001` applied — 18 surveys + 16 live bookings repaired): editing client name/email/phone or site address in the lead drawer syncs the linked surveys' denormalised columns, provisional/scheduled bookings (calendar), and the customers row via `propagateEnquiryContactDetails()` (`supabase-data.ts`). Customers row skipped with a toast when shared by other leads; quotations/reports/completed bookings stay frozen snapshots. Raised by the office admin (Bowson → Mickey Mouse example).
- 2026-07-13 — CF Project Reference (80d177f, migration `20260713000001` applied): free-text `surveys.cf_project_reference` records the manually-created Contractor Foreman project ID. Editable field on the survey hub's Survey Details card (blur/Enter save, clearable) + `CfReferencePrompt` soft modal (Save/Skip, never a hard gate) at the office's win touchpoints — Kanban drag to Won, drawer status change to Won, deposit marked paid (covers the customer-accepted auto-won path). Self-skips when no linked survey or already recorded.
- 2026-07-12 — Handover back button fixed (a5ebaca): the handover page destructured `{ goBack }` from `useSmartBack` (which returns the function directly), leaving onClick undefined — dead button, invisible to the build because type errors don't fail it. Now matches every other caller: `useSmartBack(\`/surveys/${projectId}\`)`.
- 2026-07-12 — Operations material list rebuilt to the workbook's Material-List sheet (plan: `docs/plans/2026-07-12-material-list-workbook-parity.md`): the old list showed raw engine quantities at 2dp with no units — not a workbook output. New `lib/material-purchase-list.ts` transcribes all 31 SKU purchase-rounding rules from the damp workbook's Material-List sheet (whole packs/rolls/bags + UOM + usage notes + supplier links + the sheet's no-joinery caveat), parity-gated end-to-end (oracle evaluates the real sheet cells; 15/15 PASS). Non-damp types show measurement quantities with UOM — their workbook mats sheets are "TBC" (decision for Steve, don't invent rules). Sections table + work instruction now split Warmline into its own row like the workbook's Sub Contractor Costs tab; print measurements carry units.
- 2026-07-12 — Warmline DC discoverability, review pt 4 (e799ecf): Warmline is now a first-class always-visible block in the damp wizard's Wall Treatment section (independent area input — the workbook allows it alongside membrane/tanking; the old field was buried in collapsed Plastering AND gated on membrane/tanking, which killed Steven's live survey). Report side: `METHODOLOGY_WARMLINE` sequence (draft wording awaiting Steven's sign-off), per-room scope-of-works items (damp + timber `warmline_wall_area`), room treatment label composes Warmline (sole treatment no longer renders "none"), LLM context + prompt vocabulary. No costing change — mapping/engine/templates untouched, parity 15/15 PASS.
- 2026-07-12 — Phase 3 operative outputs, review pt 15 (87dc669→b7d2915, migration `20260712000003` applied): `lib/contractor-costs.ts` reproduces the workbooks' Sub Contractor Costs columns from the same engine lines as customer pricing (materials = post-wastage pre-markup × 1.1, pay = hours × £28, travel = days × round-trip × £0.45; third-party skip/disposal rows excluded); parity runner + differ now gate per-line U/V and four contractor totals — 15/15 PASS incl. Brad Brown benchmark; three workbook contractor-column defects documented as scenario-declared deviations. New /survey/[id]/operations (admin+office): summary tiles, per-section pay/hours/materials with persisted assignment+notes (`survey_subcontractor_costs` now live), pre-job material list, printable operative work instruction (scope, measurements, pay rate, travel allowance — never customer prices/margins). Live-verified via Steel Browser incl. assignment persistence.
- 2026-07-12 — Phase 1 customer-facing fixes, all 8 items (92480e4→1bb55a9, migrations `20260712000001-2` applied): company email chain fixed at source + 42 quotation snapshots backfilled + no-store profile fetches (Next Data Cache served a stale profile on /report) + 60s branding TTL; customer quotation itemised via shared `lib/quotation-presentation.ts` (per-item prices, PSO as "Site setup, access & project management" line, reconciliation guard — /q, PDF and internal preview can no longer drift); collapsible Job Cost Summary (fixed total bar + bottom sheet < xl, in-flow card ≥ xl); /q browser resilience (plain-anchor PDF, error boundaries on /q,/pay,/report, `client_errors` log route, noscript); one combined customer email with quotation PDF attached + duplicate-send guard (409 + typed RESEND confirm + race-safe sent_at claim on all three send paths) + Reply-To actually set (Resend SDK wants `replyTo`, snake_case was silently dropped); wizard SurveyContextHeader (both addresses, booking, internal notes — offline via mirror-side `surveyContext`) + New Lead correspondence-address checkbox; custom external defects with stable-id photo linking + report subsection; `company_locations` table + data-driven ReportFooter + /settings/company management UI. ESLint made functional (.eslintrc.json — 0 errors). Verified live: curl/UA matrix on public pages, Steel Browser phone-viewport pass on costing + wizard; parity 15/15 PASS.