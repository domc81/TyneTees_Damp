# TyneTees Damp — Project State

**Last updated:** 2026-07-11
**Last commit:** 49786de — feat: full pricing-control wiring — every workbook price admin-editable
**Current phase:** Costing golden-master parity COMPLETE (all 4 workbooks, 15/15 scenarios); pricing fully admin-wired (client self-serve); Steve evidence pack ready; Phase 1 customer-facing fixes unblocked

## Current focus

**Costing engine trust restored (2026-07-11, review response).** Steven's 36pp technical review (`docs/DOM_Survey_Report_and_Costing_Platform_Technical_Review.pdf`) found the costing layer untrusted (£638.41 benchmark variance, abandoned live survey). Response, all shipped same day: golden-master parity harness (`parity/`, README there) → rates audit (0 wrong primary rates; 70 wastage misapplications + truncations fixed, migration 1) → code batches 2-4 (DPC algebra + thickness-in-metres, resin components, tape/technoseal/rope/bags, core-hole independence) → full-coverage campaign (every priced row of all 4 workbooks capturable + penny-exact, migrations 2-5) → admin backend audit (dead controls removed) → **pricing control fully admin-wired** (map + gap fixes: `docs/workbook-analysis/PRICING_CONTROL_MAP.md`, migration 6 — the client now edits every price at /admin/rates, /admin/materials, /admin/costing without developer involvement, the workbooks' core complaint). **The parity suite is the release gate for any costing change** (commands in `AGENTS.md`). Evidence pack for Steve: `parity/audit/` — incl. two workbook defects to report (damp totals omit its asbestos section; woodworm prep hours subtotal omits wallpaper+antinox).

**Next up:** send Steve the evidence pack + book the working session; execute the Phase 1 customer-facing fixes plan (`docs/plans/2026-07-11-phase1-customer-facing-fixes.md` — approved, was deferred for the costing work); then review Phase 3 operative/subcontractor outputs (oracle already emits the workbook's contractor columns as expected values) and payment staging (review pt 16).

**Canonical path (2026-07-05):** every survey starts as a lead in the pipeline (`/survey/new` deleted); repeat customers via existing-customer search, free revisits via Convert & Book "No survey fee". /surveys ("Projects") is purely the view/manage list.

## Open threads

- **Offline PWA — team testing phase (started 2026-07-06):** iPhone T10 passed after the bc75793 fix round (airplane-mode open + tap-through into downloaded surveys verified by Dominic). Still to validate with Steve's team — T10 on Android (primary platform), T14 (rural field pilot; capture sync-pill comprehension; if the stale nudge fires often, consider Background Sync API as an Android-only enhancement), T15 (real Android tablet pass of all 5 steps + PhotoCapture + AudioRecorder + pill), and real mic→queue voice-note recording (couldn't be exercised headlessly — the transcription-flush half is verified). Offer stands to add a "Working offline — daily routine" section to the surveyor training guide once the process settles.
- **CF replacement (PARKED 2026-07-05 — revisit from late July 2026):** confirmed direction — this platform will replace Contractor Foreman entirely once its used feature set is mapped and built. Agreed approach when resumed: audit-first — map the client's live CF account via Steel Browser (module inventory with usage evidence, workflow maps, data/migration inventory → `docs/cf-audit/`), then gap-analyse against the platform, then a phased build plan (likely project/works management first; invoicing + payments second, blocked on Steven's answers: bookkeeping software, payment provider, invoicing structure, operative count, offline needs). Explicitly decided against: Steel Browser automation of CF project creation from the handover pack (feasible but throwaway against a product being retired). The CSV handover pack remains the bridge. Prerequisites to resume: CF login credentials (`~/.credentials/.contractor-foreman-credentials`), 2FA status, a supervised first session at browser.dc81.io.
- **Phase 1 customer-facing fixes (review pts 8,2,3,1,10,12,13 + 9):** plan approved at `docs/plans/2026-07-11-phase1-customer-facing-fixes.md`, execution unblocked now costing parity is done. Item 1 (developer email on quotations — data fix + snapshot backfill) should ship first.
- **Review phases still open:** Phase 3 operative/subcontractor outputs (pt 15 — `contractor_hourly_rate` reserved for it; parity oracle already emits workbook U/V contractor columns as expected values); payment staging (pt 16 — 30% / 50%-of-remainder / final 50%, kill "deposit" wording); Warmline as a wall-treatment option in the wizard UX (pt 4 — pricing correct, discoverability ask).
- **Costing follow-ups (parity-gated, see `parity/audit/CAPTURE_GAPS.md`):** catalog re-link of the 7 deactivated rows is **on-client-demand only, 4 of 7 max** (decided 2026-07-11 — rationale in `docs/workbook-analysis/PRICING_CONTROL_MAP.md` §3; the lines are already fully editable at /admin/costing); `price_divisor` param for the 6 pack-representation quirks; digital-DPC radius/construction record fields; line-level override UI (engine supports `input.overrides`, no UI).
- **Legacy survey data:** TT-2026-0029 still stores the halved 20 m² resin workaround + `dpc_wall_depth: 1` courses — surveyor should re-enter (wizard prompts); other pre-parity surveys recost via documented legacy fallbacks until edited.
- **Survey type refactor:** `survey_type` enum includes `structural`, `comprehensive`, `site_preparation` — `site_preparation` has 3 costing sections but no wizard steps or report templates; the other two have nothing. Selecting them creates dead-end surveys. Plan exists at `docs/plans/2026-03-02-survey-type-display-migration.md`.
- **Role-based RLS tightening:** most tables grant full access to all authenticated users. App-level route guards (RoleGuard, ProtectedRoute allowedRoles) and API role checks now block surveyors from admin/office pages and sensitive endpoints. RLS policy migration deferred — acceptable for small team but must fix before team growth.
- **API route role checks:** payment mark-paid/send-link, quotation generation, company profile, and logo endpoints now require admin/office role (fixed in both audits). LLM/transcription endpoints remain open to all authenticated users (acceptable — no PII exposure, API credit risk is low with single-user team).
- **Supabase Auth SMTP not configured:** `GOTRUE_SMTP_HOST`, `GOTRUE_SMTP_USER`, `GOTRUE_SMTP_PASS`, `GOTRUE_SMTP_ADMIN_EMAIL` are all blank on the TTDP auth container. Password reset emails (`resetPasswordForEmail()`) silently fail. Requires root-session fix to update Coolify environment variables with SMTP credentials (Resend SMTP is available). Workaround: admin resets passwords via Supabase Admin API + `must_change_password` flag.
- **Payment provider integration:** deliberately deferred — client is not ready. Survey fees are paid through the client's external payment system and office marks them paid manually; deposits are taken in Contractor Foreman after handover. The `/pay/[token]` page and `/api/payments/send-link` route stay dormant until a provider is integrated (fix send-link's empty `appUrl` fallback before first activation). Architecture is Stripe-ready (fields in payments table).
- **UX audit follow-ups:** both audits fully closed — every platform and customer finding fixed and verified live (see Recently shipped; reports in `docs/audits/`). Untested remainder from the customer audit: email bodies (no inbox access), decline path end-to-end, expired-link states, quote PDF/print output — candidates for a future pass. Steven Robinson duplicates deduped 2026-07-05 (history re-pointed to canonical profile `8bb49fa5`; two test profiles + auth users deleted). Training guides still describe pre-unification pipeline columns in places — content refresh needed.
- **Release-unpaid-bookings cron:** `/api/cron/release-unpaid-bookings` needs a Coolify scheduled task (daily, e.g. 09:00) calling `POST` with `Authorization: Bearer $CRON_SECRET`.

## Known issues

- **13 survey-type extension tables unused:** schema provisioned but wizard uses JSONB instead. Candidates for removal.
- **RLS policies still grant full access:** `USING (true) WITH CHECK (true)` on core tables. App-level guards mitigate risk but DB-level isolation needs a dedicated migration session.
- **ESLint not configured:** `npm run lint` prompts for setup. Build validates compilation but no automated lint rules enforced.

## Recently shipped

- 2026-07-11 — Pricing control wiring + workbook→platform map (49786de): verified all 4 workbooks' pricing inputs against the three admin areas — new map `docs/workbook-analysis/PRICING_CONTROL_MAP.md`. whole_pack + DPC cream/drill now price catalog-FIRST (params demoted to fallback snapshots; EP40 ×1.1 → wastage_factor; drill plugs restated pack-of-100 £4.29); template `is_active` actually enforced; CF CSV rate + travel 6.5 h/day + 30 mph now from `pricing_config`; section default adj % + whole_pack/minimum/labour-block params editable at /admin/costing; compound/DPC material references detected on delete. Migration `20260711000006` applied; parity 15/15 PASS, zero price change.
- 2026-07-11 — Wizard UI for full workbook capture + admin backend audit (955636c): new capture fields across all four issue types' wizard components (grouped, tablet-first, blank-preserving legacy fallbacks); admin audit removed dead rate controls (asbestos/digital-DPC config rows deleted — templates govern), relabelled `contractor_hourly_rate` reserved, deactivated 7 no-op catalog rows, VAT now reads `pricing_config.vat_rate`. Report: `parity/audit/ADMIN_AUDIT.md`.
- 2026-07-11 — Full-coverage campaign (a71be06→34443ee, migrations 2-4): every priced input row of all 4 workbooks capturable + penny-exact — 15/15 scenarios PASS incl. 6 generated full-coverage scenarios. New `whole_pack` formula; PIV-loft −5% default dial; more invented rules removed (skip-vs-disposal exclusivity, CVent core-hole bundle, multi-height membrane lumping, timber strip=install coupling); fixing rope/technoseal/overtape derivations recovered; two WORKBOOK summation defects documented as deliberate deviations (`parity/audit/CAPTURE_GAPS.md`).
- 2026-07-11 — Costing parity batches 1-4 (9f7ce1d→2deecf0): rates audit fixed 70 wastage misapplications + 16 truncations at source (schema widened — old numeric scales couldn't store exact values); DPC rebuilt to workbook algebra with free-numeric thickness-in-metres (£907.20 → £490.52 on Steven's exact inputs); resin components independent (£538.98 → £280.20); tape ÷2.5, technoseal emitted, fractional bags, days-without-distance; condensation/timber/woodworm cellmaps + point-11 core-hole acceptance scenario passing.
- 2026-07-11 — Client review response foundation (4da6aa0→9e89eab): Steven's technical review committed + Phase 1 plan (`docs/plans/2026-07-11-phase1-customer-facing-fixes.md`); golden-master parity harness built (`parity/` — workbook oracle via `formulas`, live-pipeline runner, penny differ); Brad Brown scenario reproduces his review screenshots bit-for-bit and decomposed the £638.41 variance to 9 attributed defects.
- 2026-07-06 — Historical-survey enquiry backfill (migration `20260706000001`, applied): the 22 pre-pipeline surveys (no `enquiry_id`) were invisible to the Kanban and the handover pack (gates on enquiry won/closed). One enquiry per survey backfilled (`source = 'Historical Import'`, `internal_reference` = project number, CF numbering continued per type; completed surveys → Survey Complete lane, the 4 unfinished → Booked). Zero orphan surveys remain; rollback = delete by source + re-null `surveys.enquiry_id`.
- 2026-07-06 — Offline PWA field-test fix round (bc75793→8fffbf0), iPhone airplane-mode verified after: offline taps now land on the wizard not the online-only `/surveys/<id>` hub (offline cards hard-link `<a>`; hub falls back to wizard when a mirror exists); SEED_URLS posts via `serviceWorker.ready` (gating on `.controller` skipped all page seeding for the first session after install) and seeds hub+wizard per survey; manual per-survey Download pill (`downloadSurveyOffline()`) covers surveys outside the today+tomorrow booking window; Download/Downloaded/Unsynced upgraded to prominent SyncStatusPill-style pills; nav tabs reordered by use frequency (role filtering gives surveyors Surveys/Calendar on top, office Pipeline first). Gotcha recorded in `AGENTS.md`.
- 2026-07-06 — Offline-first PWA for the survey wizard (d75a3e0→76af5b1, 7 phases): local-first data layer (`src/lib/offline/` — Dexie `ttdp-offline`, op-based coalescing outbox, connectivity probe, sync engine reusing existing server writers), booking-driven prefetch (today+tomorrow), offline photo capture (deterministic `${surveyId}/${step}/${photo.id}.jpg` paths + upsert for idempotent retry, blob-URL rendering, ConfirmDialog delete), Serwist SW + manifest + install hint (NetworkFirst pages, CacheFirst photos, NetworkOnly PostgREST/auth/api + public /q,/pay,/report, SEED_URLS for cold launch), deferred voice-note transcription, sync pill + Downloaded badge + stale banner + logout-clears-DB guard, profile cache for offline role/id. Verified T1–T9,T11–T13 on live deploy. Module map: `docs/ARCHITECTURE.md`; plan: `docs/plans/2026-07-05-offline-first-pwa-survey-wizard.md`.
- 2026-07-05 — Canonical survey-creation path (b3aee8f): pipeline-only. `/survey/new` + `createSurveyFromForm()` deleted; dashboard/Projects buttons → "New Lead" → `/enquiries?new=1` deep link (admin/office only). New Lead form: existing-customer search (name/email/phone/postcode) prefills contacts and links `customer_id` — no duplicate customers for repeat business. Convert & Book: "No survey fee" option for free revisits (booking created `scheduled`, lead straight to Booked, no payment record). Training guides updated.
- 2026-07-05 — Customer UX audit fix batch (b460f87): all 12 findings + 3 addenda fixed and verified live. force-dynamic on tokenized pages; /pay contact columns fixed; ground-levels checklist item + honest report logic; generation-time LLM polish of raw narrative (raw kept in data.raw_notes); WME standardised; quote → names + single Works subtotal + scope note (page + PDF); reported-defect fallback chain (filler removed); project_number in report footer; 409 messages state prior response; ConfirmDialog component (report finalise/unpublish); publish gated until finalised; won_at stamped on public accept. Also fixed a leftover `user` ref in handover useCallback deps (ReferenceError from 27f203c). Audit itself: `docs/audits/UX_AUDIT_customer_2026-07-05.md`.
